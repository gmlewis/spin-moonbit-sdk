// Generated by `wit-bindgen` 0.29.0. DO NOT EDIT!
/// An open key-value store
pub type Store Int derive(Show, Eq)

fn wasmImportResourceDropStore(resource : Int) = "gmlewis:spin-moonbit-pdk/key-value" "[resource-drop]store"

pub fn Store::drop(self : Store) -> Unit {
  wasmImportResourceDropStore(self.0)
}

/// The set of errors which may be raised by functions in this interface
pub enum Error_ {
  StoreTableFull
  NoSuchStore
  AccessDenied
  Other(String)
} derive(Show, Eq)

/// Open the store with the specified label.
///
/// `label` must refer to a store allowed in the spin.toml manifest.
///
/// `error::no-such-store` will be raised if the `label` is not recognized.
pub fn Store::open(label : String) -> Result[Store, Error_] {
  let return_area = @ffi.malloc(16)
  wasmImportStaticStoreOpen(
    @ffi.str2ptr(label),
    label.iter().count(),
    return_area,
  )
  let lifted10 = match @ffi.load8_u(return_area + 0) {
    0 => Result::Ok(Store::Store(@ffi.load32(return_area + 4)))
    1 => {
      let lifted = match @ffi.load8_u(return_area + 4) {
        0 => Error_::StoreTableFull
        1 => Error_::NoSuchStore
        2 => Error_::AccessDenied
        3 => {
          let result = @ffi.ptr2str(@ffi.load32(return_area + 8))
          Error_::Other(result)
        }
        _ => panic()
      }
      Result::Err(lifted)
    }
    _ => panic()
  }
  ignore(label)
  @ffi.free(return_area)
  return lifted10
}

/// Get the value associated with the specified `key`
///
/// Returns `ok(none)` if the key does not exist.
pub fn Store::get(self : Store, key : String) -> Result[Bytes?, Error_] {
  let return_area = @ffi.malloc(16)
  wasmImportMethodStoreGet(
    self.0,
    @ffi.str2ptr(key),
    key.iter().count(),
    return_area,
  )
  let lifted16 = match @ffi.load8_u(return_area + 0) {
    0 => {
      let lifted : Bytes? = match @ffi.load8_u(return_area + 4) {
        0 => Option::None
        1 => {
          let result = @ffi.ptr2bytes(@ffi.load32(return_area + 8))
          Option::Some(result)
        }
        _ => panic()
      }
      Result::Ok(lifted)
    }
    1 => {
      let lifted15 = match @ffi.load8_u(return_area + 4) {
        0 => Error_::StoreTableFull
        1 => Error_::NoSuchStore
        2 => Error_::AccessDenied
        3 => {
          let result14 = @ffi.ptr2str(@ffi.load32(return_area + 8))
          Error_::Other(result14)
        }
        _ => panic()
      }
      Result::Err(lifted15)
    }
    _ => panic()
  }
  ignore(key)
  @ffi.free(return_area)
  return lifted16
}

/// Set the `value` associated with the specified `key` overwriting any existing value.
pub fn Store::set(
  self : Store,
  key : String,
  value : Bytes
) -> Result[Unit, Error_] {
  let return_area = @ffi.malloc(16)
  wasmImportMethodStoreSet(
    self.0,
    @ffi.str2ptr(key),
    key.iter().count(),
    @ffi.bytes2ptr(value),
    value.length(),
    return_area,
  )
  let lifted10 = match @ffi.load8_u(return_area + 0) {
    0 => Result::Ok(())
    1 => {
      let lifted = match @ffi.load8_u(return_area + 4) {
        0 => Error_::StoreTableFull
        1 => Error_::NoSuchStore
        2 => Error_::AccessDenied
        3 => {
          let result = @ffi.ptr2str(@ffi.load32(return_area + 8))
          Error_::Other(result)
        }
        _ => panic()
      }
      Result::Err(lifted)
    }
    _ => panic()
  }
  ignore(key)
  ignore(value)
  @ffi.free(return_area)
  return lifted10
}

/// Delete the tuple with the specified `key`
///
/// No error is raised if a tuple did not previously exist for `key`.
pub fn Store::delete(self : Store, key : String) -> Result[Unit, Error_] {
  let return_area = @ffi.malloc(16)
  wasmImportMethodStoreDelete(
    self.0,
    @ffi.str2ptr(key),
    key.iter().count(),
    return_area,
  )
  let lifted10 = match @ffi.load8_u(return_area + 0) {
    0 => Result::Ok(())
    1 => {
      let lifted = match @ffi.load8_u(return_area + 4) {
        0 => Error_::StoreTableFull
        1 => Error_::NoSuchStore
        2 => Error_::AccessDenied
        3 => {
          let result = @ffi.ptr2str(@ffi.load32(return_area + 8))
          Error_::Other(result)
        }
        _ => panic()
      }
      Result::Err(lifted)
    }
    _ => panic()
  }
  ignore(key)
  @ffi.free(return_area)
  return lifted10
}

/// Return whether a tuple exists for the specified `key`
pub fn Store::exists(self : Store, key : String) -> Result[Bool, Error_] {
  let return_area = @ffi.malloc(16)
  wasmImportMethodStoreExists(
    self.0,
    @ffi.str2ptr(key),
    key.iter().count(),
    return_area,
  )
  let lifted10 = match @ffi.load8_u(return_area + 0) {
    0 => Result::Ok(@ffi.load8_u(return_area + 4) != 0)
    1 => {
      let lifted = match @ffi.load8_u(return_area + 4) {
        0 => Error_::StoreTableFull
        1 => Error_::NoSuchStore
        2 => Error_::AccessDenied
        3 => {
          let result = @ffi.ptr2str(@ffi.load32(return_area + 8))
          Error_::Other(result)
        }
        _ => panic()
      }
      Result::Err(lifted)
    }
    _ => panic()
  }
  ignore(key)
  @ffi.free(return_area)
  return lifted10
}

/// Return a list of all the keys
pub fn Store::get_keys(self : Store) -> Result[Array[String], Error_] {
  let return_area = @ffi.malloc(16)
  wasmImportMethodStoreGetKeys(self.0, return_area)
  let lifted13 = match @ffi.load8_u(return_area + 0) {
    0 => {
      let array : Array[String] = []
      for index = 0; index < @ffi.load32(return_area + 8); index = index + 1 {
        let base1 = @ffi.load32(return_area + 4) + index * 8
        let result = @ffi.ptr2str(@ffi.load32(base1 + 0))
        array.push(result)
      }
      @ffi.free(@ffi.load32(return_area + 4))
      Result::Ok(array)
    }
    1 => {
      let lifted = match @ffi.load8_u(return_area + 4) {
        0 => Error_::StoreTableFull
        1 => Error_::NoSuchStore
        2 => Error_::AccessDenied
        3 => {
          let result12 = @ffi.ptr2str(@ffi.load32(return_area + 8))
          Error_::Other(result12)
        }
        _ => panic()
      }
      Result::Err(lifted)
    }
    _ => panic()
  }
  @ffi.free(return_area)
  return lifted13
}
