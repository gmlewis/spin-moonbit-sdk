// Generated by `wit-bindgen` 0.62.0. DO NOT EDIT!

///|
/// Errors related to interacting with Mqtt
pub(all) enum Error_ {
  InvalidAddress
  TooManyConnections
  ConnectionFailed(String)
  Other(String)
} derive(Debug, Eq)

///|
/// QoS for publishing Mqtt messages
pub(all) enum Qos {
  AT_MOST_ONCE
  AT_LEAST_ONCE
  EXACTLY_ONCE
} derive(Debug, Eq)

///|
pub fn Qos::ordinal(self : Qos) -> Int {
  match self {
    AT_MOST_ONCE => 0
    AT_LEAST_ONCE => 1
    EXACTLY_ONCE => 2
  }
}

///|
pub fn Qos::from(self : Int) -> Qos {
  match self {
    0 => AT_MOST_ONCE
    1 => AT_LEAST_ONCE
    2 => EXACTLY_ONCE
    _ => panic()
  }
}

///|
pub(all) struct Connection(Int) derive(Debug, Eq)

///|
/// Drops a resource handle.
pub fn Connection::drop(self : Connection) -> Unit {
  let Connection(resource) = self
  wasmImportResourceDropConnection(resource)
}

///|
/// Open a connection to the Mqtt instance at `address`.
pub fn Connection::open(
  address : String,
  username : String,
  password : String,
  keep_alive_interval_in_secs : UInt64,
) -> Result[Connection, Error_] {
  let ptr = @ffi.str2ptr(address)

  let ptr0 = @ffi.str2ptr(username)

  let ptr1 = @ffi.str2ptr(password)
  let return_area = @ffi.malloc(16)
  wasmImportStaticConnectionOpen(
    ptr,
    address.length(),
    ptr0,
    username.length(),
    ptr1,
    password.length(),
    keep_alive_interval_in_secs.reinterpret_as_int64(),
    return_area,
  )

  let lifted3 = match @ffi.load8_u(return_area + 0) {
    0 => Result::Ok(Connection::Connection(@ffi.load32(return_area + 4)))
    1 => {
      let lifted = match @ffi.load8_u(return_area + 4) {
        0 => Error_::InvalidAddress
        1 => Error_::TooManyConnections
        2 => {
          let result = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          Error_::ConnectionFailed(result)
        }
        3 => {
          let result2 = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          Error_::Other(result2)
        }
        _ => panic()
      }

      Result::Err(lifted)
    }
    _ => panic()
  }
  let ret = lifted3
  @ffi.free(ptr)
  @ffi.free(ptr0)
  @ffi.free(ptr1)
  @ffi.free(return_area)
  return ret
}

///|
/// Publish an Mqtt message to the specified `topic`.
pub fn Connection::publish(
  self : Connection,
  topic : String,
  payload : FixedArray[Byte],
  qos : Qos,
) -> Result[Unit, Error_] {
  let Connection(handle) = self

  let ptr = @ffi.str2ptr(topic)

  let ptr0 = @ffi.bytes2ptr(payload)

  let return_area = @ffi.malloc(16)
  wasmImportMethodConnectionPublish(
    handle,
    ptr,
    topic.length(),
    ptr0,
    payload.length(),
    qos.ordinal(),
    return_area,
  )

  let lifted2 = match @ffi.load8_u(return_area + 0) {
    0 => Result::Ok(())
    1 => {
      let lifted = match @ffi.load8_u(return_area + 4) {
        0 => Error_::InvalidAddress
        1 => Error_::TooManyConnections
        2 => {
          let result = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          Error_::ConnectionFailed(result)
        }
        3 => {
          let result1 = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          Error_::Other(result1)
        }
        _ => panic()
      }

      Result::Err(lifted)
    }
    _ => panic()
  }
  let ret = lifted2
  @ffi.free(ptr)
  @ffi.free(ptr0)
  @ffi.free(return_area)
  return ret
}
