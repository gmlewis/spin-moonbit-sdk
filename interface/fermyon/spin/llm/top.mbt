// Generated by `wit-bindgen` 0.62.0. DO NOT EDIT!

///|
/// Inference request parameters
pub(all) struct InferencingParams {
  max_tokens : UInt
  repeat_penalty : Float
  repeat_penalty_last_n_token_count : UInt
  temperature : Float
  top_k : UInt
  top_p : Float
} derive(Debug, Eq)

///|
/// The set of errors which may be raised by functions in this interface
pub(all) enum Error_ {
  ModelNotSupported
  RuntimeError(String)
  InvalidInput(String)
} derive(Debug, Eq)

///|
/// Usage information related to the inferencing result
pub(all) struct InferencingUsage {
  prompt_token_count : UInt
  generated_token_count : UInt
} derive(Debug, Eq)

///|
/// An inferencing result
pub(all) struct InferencingResult {
  text : String
  usage : InferencingUsage
} derive(Debug, Eq)

///|
/// Usage related to an embeddings generation request
pub(all) struct EmbeddingsUsage {
  prompt_token_count : UInt
} derive(Debug, Eq)

///|
/// Result of generating embeddings
pub(all) struct EmbeddingsResult {
  embeddings : Array[FixedArray[Float]]
  usage : EmbeddingsUsage
} derive(Debug, Eq)

///|
/// Perform inferencing using the provided model and prompt with the given optional params
pub fn infer(
  model : String,
  prompt : String,
  params : InferencingParams?,
) -> Result[InferencingResult, Error_] {
  let ptr = @ffi.str2ptr(model)

  let ptr0 = @ffi.str2ptr(prompt)

  let (lowered, lowered2, lowered3, lowered4, lowered5, lowered6, lowered7) = match
    params {
    None => (0, 0, (0.0 : Float), 0, (0.0 : Float), 0, (0.0 : Float))
    Some(payload1) =>
      (
        1,
        payload1.max_tokens.reinterpret_as_int(),
        payload1.repeat_penalty,
        payload1.repeat_penalty_last_n_token_count.reinterpret_as_int(),
        payload1.temperature,
        payload1.top_k.reinterpret_as_int(),
        payload1.top_p,
      )
  }
  let return_area = @ffi.malloc(20)
  wasmImportInfer(
    ptr,
    model.length(),
    ptr0,
    prompt.length(),
    lowered,
    lowered2,
    lowered3,
    lowered4,
    lowered5,
    lowered6,
    lowered7,
    return_area,
  )

  let lifted10 = match @ffi.load8_u(return_area + 0) {
    0 => {
      let result = @ffi.ptr2str(
        @ffi.load32(return_area + 4),
        @ffi.load32(return_area + 8),
      )

      Result::Ok(InferencingResult::{
        text: result,
        usage: InferencingUsage::{
          prompt_token_count: @ffi.load32(return_area + 12).reinterpret_as_uint(),
          generated_token_count: @ffi.load32(return_area + 16).reinterpret_as_uint(),
        },
      })
    }
    1 => {
      let lifted = match @ffi.load8_u(return_area + 4) {
        0 => Error_::ModelNotSupported
        1 => {
          let result8 = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          Error_::RuntimeError(result8)
        }
        2 => {
          let result9 = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          Error_::InvalidInput(result9)
        }
        _ => panic()
      }

      Result::Err(lifted)
    }
    _ => panic()
  }
  let ret = lifted10
  @ffi.free(ptr)
  @ffi.free(ptr0)
  @ffi.free(return_area)
  return ret
}

///|
/// Generate embeddings for the supplied list of text
pub fn generate_embeddings(
  model : String,
  text : Array[String],
) -> Result[EmbeddingsResult, Error_] {
  let cleanup_list : Array[Int] = []

  let ptr = @ffi.str2ptr(model)

  let address = @ffi.malloc(text.length() * 8)
  for index = 0; index < text.length(); index = index + 1 {
    let iter_elem : String = text[index]
    let iter_base = address + index * 8

    let ptr0 = @ffi.str2ptr(iter_elem)
    @ffi.store32(iter_base + 4, iter_elem.length())
    @ffi.store32(iter_base + 0, ptr0)
    cleanup_list.push(ptr0)
  }
  let return_area = @ffi.malloc(16)
  wasmImportGenerateEmbeddings(
    ptr,
    model.length(),
    address,
    text.length(),
    return_area,
  )

  let lifted4 = match @ffi.load8_u(return_area + 0) {
    0 => {
      let array : Array[FixedArray[Float]] = []
      for index1 = 0; index1 < @ffi.load32(return_area + 8); index1 = index1 + 1 {
        let iter_base = @ffi.load32(return_area + 4) + index1 * 8

        let result = @ffi.ptr2float_array(
          @ffi.load32(iter_base + 0),
          @ffi.load32(iter_base + 4),
        )

        array.push(result)
      }
      @ffi.free(@ffi.load32(return_area + 4))

      Result::Ok(EmbeddingsResult::{
        embeddings: array,
        usage: EmbeddingsUsage::{
          prompt_token_count: @ffi.load32(return_area + 12).reinterpret_as_uint(),
        },
      })
    }
    1 => {
      let lifted = match @ffi.load8_u(return_area + 4) {
        0 => Error_::ModelNotSupported
        1 => {
          let result2 = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          Error_::RuntimeError(result2)
        }
        2 => {
          let result3 = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          Error_::InvalidInput(result3)
        }
        _ => panic()
      }

      Result::Err(lifted)
    }
    _ => panic()
  }
  let ret = lifted4
  @ffi.free(ptr)
  @ffi.free(address)
  @ffi.free(return_area)

  cleanup_list.each(@ffi.free)
  return ret
}
