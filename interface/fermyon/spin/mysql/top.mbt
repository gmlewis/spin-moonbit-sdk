// Generated by `wit-bindgen` 0.62.0. DO NOT EDIT!

///|
/// A connection to a MySQL database.
pub(all) struct Connection(Int) derive(Debug, Eq)

///|
pub extend Connection with @debug.Debug::{to_repr}

///|
pub extend Connection with Eq::{equal}

///|
#deprecated("Use `!=` instead", skip_current_package=true)
#doc(hidden)
pub extend Connection with Eq::{not_equal}

///|
/// Drops a resource handle.
pub fn Connection::drop(self : Connection) -> Unit {
  let Connection(resource) = self
  wasmImportResourceDropConnection(resource)
}

///|
/// Open a connection to the MySQL instance at `address`.
pub fn Connection::open(
  address : String,
) -> Result[Connection, @rdbmsTypes.Error_] {
  let ptr = @ffi.str2ptr(address)
  let return_area = @ffi.malloc(16)
  wasmImportStaticConnectionOpen(ptr, address.length(), return_area)

  let lifted4 = match @ffi.load8_u(return_area + 0) {
    0 => Result::Ok(Connection::Connection(@ffi.load32(return_area + 4)))
    1 => {
      let lifted = match @ffi.load8_u(return_area + 4) {
        0 => {
          let result = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          @rdbmsTypes.Error_::ConnectionFailed(result)
        }
        1 => {
          let result0 = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          @rdbmsTypes.Error_::BadParameter(result0)
        }
        2 => {
          let result1 = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          @rdbmsTypes.Error_::QueryFailed(result1)
        }
        3 => {
          let result2 = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          @rdbmsTypes.Error_::ValueConversionFailed(result2)
        }
        4 => {
          let result3 = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          @rdbmsTypes.Error_::Other(result3)
        }
        _ => panic()
      }

      Result::Err(lifted)
    }
    _ => panic()
  }
  let ret = lifted4
  @ffi.free(ptr)
  @ffi.free(return_area)
  return ret
}

///|
/// query the database: select
pub fn Connection::query(
  self : Connection,
  statement : String,
  params : Array[@rdbmsTypes.ParameterValue],
) -> Result[@rdbmsTypes.RowSet, @rdbmsTypes.Error_] {
  let cleanup_list : Array[Int] = []

  let Connection(handle) = self

  let ptr = @ffi.str2ptr(statement)

  let address = @ffi.malloc(params.length() * 16)
  for index = 0; index < params.length(); index = index + 1 {
    let iter_elem : @rdbmsTypes.ParameterValue = params[index]
    let iter_base = address + index * 16

    match iter_elem {
      Boolean(payload) => {
        @ffi.store8(iter_base + 0, 0)
        @ffi.store8(iter_base + 8, if payload { 1 } else { 0 })

        ()
      }
      Int8(payload0) => {
        @ffi.store8(iter_base + 0, 1)
        @ffi.store8(iter_base + 8, @ffi.extend8(payload0))

        ()
      }
      Int16(payload1) => {
        @ffi.store8(iter_base + 0, 2)
        @ffi.store16(iter_base + 8, @ffi.extend16(payload1))

        ()
      }
      Int32(payload2) => {
        @ffi.store8(iter_base + 0, 3)
        @ffi.store32(iter_base + 8, payload2)

        ()
      }
      Int64(payload3) => {
        @ffi.store8(iter_base + 0, 4)
        @ffi.store64(iter_base + 8, payload3)

        ()
      }
      Uint8(payload4) => {
        @ffi.store8(iter_base + 0, 5)
        @ffi.store8(iter_base + 8, payload4.to_int())

        ()
      }
      Uint16(payload5) => {
        @ffi.store8(iter_base + 0, 6)
        @ffi.store16(iter_base + 8, payload5.reinterpret_as_int())

        ()
      }
      Uint32(payload6) => {
        @ffi.store8(iter_base + 0, 7)
        @ffi.store32(iter_base + 8, payload6.reinterpret_as_int())

        ()
      }
      Uint64(payload7) => {
        @ffi.store8(iter_base + 0, 8)
        @ffi.store64(iter_base + 8, payload7.reinterpret_as_int64())

        ()
      }
      Floating32(payload8) => {
        @ffi.store8(iter_base + 0, 9)
        @ffi.storef32(iter_base + 8, payload8)

        ()
      }
      Floating64(payload9) => {
        @ffi.store8(iter_base + 0, 10)
        @ffi.storef64(iter_base + 8, payload9)

        ()
      }
      Str(payload10) => {
        @ffi.store8(iter_base + 0, 11)

        let ptr11 = @ffi.str2ptr(payload10)
        @ffi.store32(iter_base + 12, payload10.length())
        @ffi.store32(iter_base + 8, ptr11)
        cleanup_list.push(ptr11)

        ()
      }
      Binary(payload12) => {
        @ffi.store8(iter_base + 0, 12)

        let ptr13 = @ffi.bytes2ptr(payload12)

        @ffi.store32(iter_base + 12, payload12.length())
        @ffi.store32(iter_base + 8, ptr13)
        cleanup_list.push(ptr13)

        ()
      }
      DbNull => {
        @ffi.store8(iter_base + 0, 13)

        ()
      }
    }
  }
  let return_area = @ffi.malloc(20)
  wasmImportMethodConnectionQuery(
    handle,
    ptr,
    statement.length(),
    address,
    params.length(),
    return_area,
  )

  let lifted28 = match @ffi.load8_u(return_area + 0) {
    0 => {
      let array : Array[@rdbmsTypes.Column] = []
      for index15 = 0
          index15 < @ffi.load32(return_area + 8)
          index15 = index15 + 1 {
        let iter_base = @ffi.load32(return_area + 4) + index15 * 12

        let result = @ffi.ptr2str(
          @ffi.load32(iter_base + 0),
          @ffi.load32(iter_base + 4),
        )

        array.push(@rdbmsTypes.Column::{
          name: result,
          data_type: @rdbmsTypes.DbDataType::from(@ffi.load8_u(iter_base + 8)),
        })
      }
      @ffi.free(@ffi.load32(return_area + 4))

      let array20 : Array[Array[@rdbmsTypes.DbValue]] = []
      for index21 = 0
          index21 < @ffi.load32(return_area + 16)
          index21 = index21 + 1 {
        let iter_base = @ffi.load32(return_area + 12) + index21 * 8

        let array18 : Array[@rdbmsTypes.DbValue] = []
        for index19 = 0
            index19 < @ffi.load32(iter_base + 4)
            index19 = index19 + 1 {
          let iter_base = @ffi.load32(iter_base + 0) + index19 * 16

          let lifted = match @ffi.load8_u(iter_base + 0) {
            0 => @rdbmsTypes.DbValue::Boolean(@ffi.load8_u(iter_base + 8) != 0)
            1 => @rdbmsTypes.DbValue::Int8(@ffi.load8(iter_base + 8) - 0x100)
            2 =>
              @rdbmsTypes.DbValue::Int16(@ffi.load16(iter_base + 8) - 0x10000)
            3 => @rdbmsTypes.DbValue::Int32(@ffi.load32(iter_base + 8))
            4 => @rdbmsTypes.DbValue::Int64(@ffi.load64(iter_base + 8))
            5 =>
              @rdbmsTypes.DbValue::Uint8(@ffi.load8_u(iter_base + 8).to_byte())
            6 =>
              @rdbmsTypes.DbValue::Uint16(
                @ffi.load16_u(iter_base + 8).land(0xFFFF).reinterpret_as_uint(),
              )
            7 =>
              @rdbmsTypes.DbValue::Uint32(
                @ffi.load32(iter_base + 8).reinterpret_as_uint(),
              )
            8 =>
              @rdbmsTypes.DbValue::Uint64(
                @ffi.load64(iter_base + 8).reinterpret_as_uint64(),
              )
            9 => @rdbmsTypes.DbValue::Floating32(@ffi.loadf32(iter_base + 8))
            10 => @rdbmsTypes.DbValue::Floating64(@ffi.loadf64(iter_base + 8))
            11 => {
              let result16 = @ffi.ptr2str(
                @ffi.load32(iter_base + 8),
                @ffi.load32(iter_base + 12),
              )

              @rdbmsTypes.DbValue::Str(result16)
            }
            12 => {
              let result17 = @ffi.ptr2bytes(
                @ffi.load32(iter_base + 8),
                @ffi.load32(iter_base + 12),
              )

              @rdbmsTypes.DbValue::Binary(result17)
            }
            13 => @rdbmsTypes.DbValue::DbNull
            14 => @rdbmsTypes.DbValue::Unsupported
            _ => panic()
          }

          array18.push(lifted)
        }
        @ffi.free(@ffi.load32(iter_base + 0))

        array20.push(array18)
      }
      @ffi.free(@ffi.load32(return_area + 12))

      Result::Ok(@rdbmsTypes.RowSet::{ columns: array, rows: array20, })
    }
    1 => {
      let lifted27 = match @ffi.load8_u(return_area + 4) {
        0 => {
          let result22 = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          @rdbmsTypes.Error_::ConnectionFailed(result22)
        }
        1 => {
          let result23 = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          @rdbmsTypes.Error_::BadParameter(result23)
        }
        2 => {
          let result24 = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          @rdbmsTypes.Error_::QueryFailed(result24)
        }
        3 => {
          let result25 = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          @rdbmsTypes.Error_::ValueConversionFailed(result25)
        }
        4 => {
          let result26 = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          @rdbmsTypes.Error_::Other(result26)
        }
        _ => panic()
      }

      Result::Err(lifted27)
    }
    _ => panic()
  }
  let ret = lifted28
  @ffi.free(ptr)
  @ffi.free(address)
  @ffi.free(return_area)

  cleanup_list.each(@ffi.free)
  return ret
}

///|
/// execute command to the database: insert, update, delete
pub fn Connection::execute(
  self : Connection,
  statement : String,
  params : Array[@rdbmsTypes.ParameterValue],
) -> Result[Unit, @rdbmsTypes.Error_] {
  let cleanup_list : Array[Int] = []

  let Connection(handle) = self

  let ptr = @ffi.str2ptr(statement)

  let address = @ffi.malloc(params.length() * 16)
  for index = 0; index < params.length(); index = index + 1 {
    let iter_elem : @rdbmsTypes.ParameterValue = params[index]
    let iter_base = address + index * 16

    match iter_elem {
      Boolean(payload) => {
        @ffi.store8(iter_base + 0, 0)
        @ffi.store8(iter_base + 8, if payload { 1 } else { 0 })

        ()
      }
      Int8(payload0) => {
        @ffi.store8(iter_base + 0, 1)
        @ffi.store8(iter_base + 8, @ffi.extend8(payload0))

        ()
      }
      Int16(payload1) => {
        @ffi.store8(iter_base + 0, 2)
        @ffi.store16(iter_base + 8, @ffi.extend16(payload1))

        ()
      }
      Int32(payload2) => {
        @ffi.store8(iter_base + 0, 3)
        @ffi.store32(iter_base + 8, payload2)

        ()
      }
      Int64(payload3) => {
        @ffi.store8(iter_base + 0, 4)
        @ffi.store64(iter_base + 8, payload3)

        ()
      }
      Uint8(payload4) => {
        @ffi.store8(iter_base + 0, 5)
        @ffi.store8(iter_base + 8, payload4.to_int())

        ()
      }
      Uint16(payload5) => {
        @ffi.store8(iter_base + 0, 6)
        @ffi.store16(iter_base + 8, payload5.reinterpret_as_int())

        ()
      }
      Uint32(payload6) => {
        @ffi.store8(iter_base + 0, 7)
        @ffi.store32(iter_base + 8, payload6.reinterpret_as_int())

        ()
      }
      Uint64(payload7) => {
        @ffi.store8(iter_base + 0, 8)
        @ffi.store64(iter_base + 8, payload7.reinterpret_as_int64())

        ()
      }
      Floating32(payload8) => {
        @ffi.store8(iter_base + 0, 9)
        @ffi.storef32(iter_base + 8, payload8)

        ()
      }
      Floating64(payload9) => {
        @ffi.store8(iter_base + 0, 10)
        @ffi.storef64(iter_base + 8, payload9)

        ()
      }
      Str(payload10) => {
        @ffi.store8(iter_base + 0, 11)

        let ptr11 = @ffi.str2ptr(payload10)
        @ffi.store32(iter_base + 12, payload10.length())
        @ffi.store32(iter_base + 8, ptr11)
        cleanup_list.push(ptr11)

        ()
      }
      Binary(payload12) => {
        @ffi.store8(iter_base + 0, 12)

        let ptr13 = @ffi.bytes2ptr(payload12)

        @ffi.store32(iter_base + 12, payload12.length())
        @ffi.store32(iter_base + 8, ptr13)
        cleanup_list.push(ptr13)

        ()
      }
      DbNull => {
        @ffi.store8(iter_base + 0, 13)

        ()
      }
    }
  }
  let return_area = @ffi.malloc(16)
  wasmImportMethodConnectionExecute(
    handle,
    ptr,
    statement.length(),
    address,
    params.length(),
    return_area,
  )

  let lifted19 = match @ffi.load8_u(return_area + 0) {
    0 => Result::Ok(())
    1 => {
      let lifted = match @ffi.load8_u(return_area + 4) {
        0 => {
          let result = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          @rdbmsTypes.Error_::ConnectionFailed(result)
        }
        1 => {
          let result15 = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          @rdbmsTypes.Error_::BadParameter(result15)
        }
        2 => {
          let result16 = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          @rdbmsTypes.Error_::QueryFailed(result16)
        }
        3 => {
          let result17 = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          @rdbmsTypes.Error_::ValueConversionFailed(result17)
        }
        4 => {
          let result18 = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          @rdbmsTypes.Error_::Other(result18)
        }
        _ => panic()
      }

      Result::Err(lifted)
    }
    _ => panic()
  }
  let ret = lifted19
  @ffi.free(ptr)
  @ffi.free(address)
  @ffi.free(return_area)

  cleanup_list.each(@ffi.free)
  return ret
}
