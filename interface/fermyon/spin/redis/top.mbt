// Generated by `wit-bindgen` 0.62.0. DO NOT EDIT!

///|
/// Errors related to interacting with Redis
pub(all) enum Error_ {
  InvalidAddress
  TooManyConnections
  TypeError
  Other(String)
} derive(Debug, Eq)

///|
pub(all) struct Connection(Int) derive(Debug, Eq)

///|
/// Drops a resource handle.
pub fn Connection::drop(self : Connection) -> Unit {
  let Connection(resource) = self
  wasmImportResourceDropConnection(resource)
}

///|
/// A parameter type for the general-purpose `execute` function.
pub(all) enum RedisParameter {
  Int64(Int64)
  Binary(FixedArray[Byte])
} derive(Debug, Eq)

///|
/// A return type for the general-purpose `execute` function.
pub(all) enum RedisResult {
  Nil
  Status(String)
  Int64(Int64)
  Binary(FixedArray[Byte])
} derive(Debug, Eq)

///|
/// Open a connection to the Redis instance at `address`.
pub fn Connection::open(address : String) -> Result[Connection, Error_] {
  let ptr = @ffi.str2ptr(address)
  let return_area = @ffi.malloc(16)
  wasmImportStaticConnectionOpen(ptr, address.length(), return_area)

  let lifted0 = match @ffi.load8_u(return_area + 0) {
    0 => Result::Ok(Connection::Connection(@ffi.load32(return_area + 4)))
    1 => {
      let lifted = match @ffi.load8_u(return_area + 4) {
        0 => Error_::InvalidAddress
        1 => Error_::TooManyConnections
        2 => Error_::TypeError
        3 => {
          let result = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          Error_::Other(result)
        }
        _ => panic()
      }

      Result::Err(lifted)
    }
    _ => panic()
  }
  let ret = lifted0
  @ffi.free(ptr)
  @ffi.free(return_area)
  return ret
}

///|
/// Publish a Redis message to the specified channel.
pub fn Connection::publish(
  self : Connection,
  channel : String,
  payload : FixedArray[Byte],
) -> Result[Unit, Error_] {
  let Connection(handle) = self

  let ptr = @ffi.str2ptr(channel)

  let ptr0 = @ffi.bytes2ptr(payload)

  let return_area = @ffi.malloc(16)
  wasmImportMethodConnectionPublish(
    handle,
    ptr,
    channel.length(),
    ptr0,
    payload.length(),
    return_area,
  )

  let lifted1 = match @ffi.load8_u(return_area + 0) {
    0 => Result::Ok(())
    1 => {
      let lifted = match @ffi.load8_u(return_area + 4) {
        0 => Error_::InvalidAddress
        1 => Error_::TooManyConnections
        2 => Error_::TypeError
        3 => {
          let result = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          Error_::Other(result)
        }
        _ => panic()
      }

      Result::Err(lifted)
    }
    _ => panic()
  }
  let ret = lifted1
  @ffi.free(ptr)
  @ffi.free(ptr0)
  @ffi.free(return_area)
  return ret
}

///|
/// Get the value of a key.
pub fn Connection::get(
  self : Connection,
  key : String,
) -> Result[FixedArray[Byte]?, Error_] {
  let Connection(handle) = self

  let ptr = @ffi.str2ptr(key)
  let return_area = @ffi.malloc(16)
  wasmImportMethodConnectionGet(handle, ptr, key.length(), return_area)

  let lifted2 = match @ffi.load8_u(return_area + 0) {
    0 => {
      let lifted : FixedArray[Byte]? = match @ffi.load8_u(return_area + 4) {
        0 => Option::None
        1 => {
          let result = @ffi.ptr2bytes(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          Option::Some(result)
        }
        _ => panic()
      }

      Result::Ok(lifted)
    }
    1 => {
      let lifted1 = match @ffi.load8_u(return_area + 4) {
        0 => Error_::InvalidAddress
        1 => Error_::TooManyConnections
        2 => Error_::TypeError
        3 => {
          let result0 = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          Error_::Other(result0)
        }
        _ => panic()
      }

      Result::Err(lifted1)
    }
    _ => panic()
  }
  let ret = lifted2
  @ffi.free(ptr)
  @ffi.free(return_area)
  return ret
}

///|
/// Set key to value.
///
/// If key already holds a value, it is overwritten.
pub fn Connection::set(
  self : Connection,
  key : String,
  value : FixedArray[Byte],
) -> Result[Unit, Error_] {
  let Connection(handle) = self

  let ptr = @ffi.str2ptr(key)

  let ptr0 = @ffi.bytes2ptr(value)

  let return_area = @ffi.malloc(16)
  wasmImportMethodConnectionSet(
    handle,
    ptr,
    key.length(),
    ptr0,
    value.length(),
    return_area,
  )

  let lifted1 = match @ffi.load8_u(return_area + 0) {
    0 => Result::Ok(())
    1 => {
      let lifted = match @ffi.load8_u(return_area + 4) {
        0 => Error_::InvalidAddress
        1 => Error_::TooManyConnections
        2 => Error_::TypeError
        3 => {
          let result = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          Error_::Other(result)
        }
        _ => panic()
      }

      Result::Err(lifted)
    }
    _ => panic()
  }
  let ret = lifted1
  @ffi.free(ptr)
  @ffi.free(ptr0)
  @ffi.free(return_area)
  return ret
}

///|
/// Increments the number stored at key by one.
///
/// If the key does not exist, it is set to 0 before performing the operation.
/// An `error::type-error` is returned if the key contains a value of the wrong type
/// or contains a string that can not be represented as integer.
pub fn Connection::incr(
  self : Connection,
  key : String,
) -> Result[Int64, Error_] {
  let Connection(handle) = self

  let ptr = @ffi.str2ptr(key)
  let return_area = @ffi.malloc(24)
  wasmImportMethodConnectionIncr(handle, ptr, key.length(), return_area)

  let lifted0 = match @ffi.load8_u(return_area + 0) {
    0 => Result::Ok(@ffi.load64(return_area + 8))
    1 => {
      let lifted = match @ffi.load8_u(return_area + 8) {
        0 => Error_::InvalidAddress
        1 => Error_::TooManyConnections
        2 => Error_::TypeError
        3 => {
          let result = @ffi.ptr2str(
            @ffi.load32(return_area + 12),
            @ffi.load32(return_area + 16),
          )

          Error_::Other(result)
        }
        _ => panic()
      }

      Result::Err(lifted)
    }
    _ => panic()
  }
  let ret = lifted0
  @ffi.free(ptr)
  @ffi.free(return_area)
  return ret
}

///|
/// Removes the specified keys.
///
/// A key is ignored if it does not exist. Returns the number of keys deleted.
pub fn Connection::del(
  self : Connection,
  keys : Array[String],
) -> Result[UInt, Error_] {
  let cleanup_list : Array[Int] = []

  let Connection(handle) = self

  let address = @ffi.malloc(keys.length() * 8)
  for index = 0; index < keys.length(); index = index + 1 {
    let iter_elem : String = keys[index]
    let iter_base = address + index * 8

    let ptr = @ffi.str2ptr(iter_elem)
    @ffi.store32(iter_base + 4, iter_elem.length())
    @ffi.store32(iter_base + 0, ptr)
    cleanup_list.push(ptr)
  }
  let return_area = @ffi.malloc(16)
  wasmImportMethodConnectionDel(handle, address, keys.length(), return_area)

  let lifted0 = match @ffi.load8_u(return_area + 0) {
    0 => Result::Ok(@ffi.load32(return_area + 4).reinterpret_as_uint())
    1 => {
      let lifted = match @ffi.load8_u(return_area + 4) {
        0 => Error_::InvalidAddress
        1 => Error_::TooManyConnections
        2 => Error_::TypeError
        3 => {
          let result = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          Error_::Other(result)
        }
        _ => panic()
      }

      Result::Err(lifted)
    }
    _ => panic()
  }
  let ret = lifted0
  @ffi.free(address)
  @ffi.free(return_area)

  cleanup_list.each(@ffi.free)
  return ret
}

///|
/// Add the specified `values` to the set named `key`, returning the number of newly-added values.
pub fn Connection::sadd(
  self : Connection,
  key : String,
  values : Array[String],
) -> Result[UInt, Error_] {
  let cleanup_list : Array[Int] = []

  let Connection(handle) = self

  let ptr = @ffi.str2ptr(key)

  let address = @ffi.malloc(values.length() * 8)
  for index = 0; index < values.length(); index = index + 1 {
    let iter_elem : String = values[index]
    let iter_base = address + index * 8

    let ptr0 = @ffi.str2ptr(iter_elem)
    @ffi.store32(iter_base + 4, iter_elem.length())
    @ffi.store32(iter_base + 0, ptr0)
    cleanup_list.push(ptr0)
  }
  let return_area = @ffi.malloc(16)
  wasmImportMethodConnectionSadd(
    handle,
    ptr,
    key.length(),
    address,
    values.length(),
    return_area,
  )

  let lifted1 = match @ffi.load8_u(return_area + 0) {
    0 => Result::Ok(@ffi.load32(return_area + 4).reinterpret_as_uint())
    1 => {
      let lifted = match @ffi.load8_u(return_area + 4) {
        0 => Error_::InvalidAddress
        1 => Error_::TooManyConnections
        2 => Error_::TypeError
        3 => {
          let result = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          Error_::Other(result)
        }
        _ => panic()
      }

      Result::Err(lifted)
    }
    _ => panic()
  }
  let ret = lifted1
  @ffi.free(ptr)
  @ffi.free(address)
  @ffi.free(return_area)

  cleanup_list.each(@ffi.free)
  return ret
}

///|
/// Retrieve the contents of the set named `key`.
pub fn Connection::smembers(
  self : Connection,
  key : String,
) -> Result[Array[String], Error_] {
  let Connection(handle) = self

  let ptr = @ffi.str2ptr(key)
  let return_area = @ffi.malloc(16)
  wasmImportMethodConnectionSmembers(handle, ptr, key.length(), return_area)

  let lifted1 = match @ffi.load8_u(return_area + 0) {
    0 => {
      let array : Array[String] = []
      for index = 0; index < @ffi.load32(return_area + 8); index = index + 1 {
        let iter_base = @ffi.load32(return_area + 4) + index * 8

        let result = @ffi.ptr2str(
          @ffi.load32(iter_base + 0),
          @ffi.load32(iter_base + 4),
        )

        array.push(result)
      }
      @ffi.free(@ffi.load32(return_area + 4))

      Result::Ok(array)
    }
    1 => {
      let lifted = match @ffi.load8_u(return_area + 4) {
        0 => Error_::InvalidAddress
        1 => Error_::TooManyConnections
        2 => Error_::TypeError
        3 => {
          let result0 = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          Error_::Other(result0)
        }
        _ => panic()
      }

      Result::Err(lifted)
    }
    _ => panic()
  }
  let ret = lifted1
  @ffi.free(ptr)
  @ffi.free(return_area)
  return ret
}

///|
/// Remove the specified `values` from the set named `key`, returning the number of newly-removed values.
pub fn Connection::srem(
  self : Connection,
  key : String,
  values : Array[String],
) -> Result[UInt, Error_] {
  let cleanup_list : Array[Int] = []

  let Connection(handle) = self

  let ptr = @ffi.str2ptr(key)

  let address = @ffi.malloc(values.length() * 8)
  for index = 0; index < values.length(); index = index + 1 {
    let iter_elem : String = values[index]
    let iter_base = address + index * 8

    let ptr0 = @ffi.str2ptr(iter_elem)
    @ffi.store32(iter_base + 4, iter_elem.length())
    @ffi.store32(iter_base + 0, ptr0)
    cleanup_list.push(ptr0)
  }
  let return_area = @ffi.malloc(16)
  wasmImportMethodConnectionSrem(
    handle,
    ptr,
    key.length(),
    address,
    values.length(),
    return_area,
  )

  let lifted1 = match @ffi.load8_u(return_area + 0) {
    0 => Result::Ok(@ffi.load32(return_area + 4).reinterpret_as_uint())
    1 => {
      let lifted = match @ffi.load8_u(return_area + 4) {
        0 => Error_::InvalidAddress
        1 => Error_::TooManyConnections
        2 => Error_::TypeError
        3 => {
          let result = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          Error_::Other(result)
        }
        _ => panic()
      }

      Result::Err(lifted)
    }
    _ => panic()
  }
  let ret = lifted1
  @ffi.free(ptr)
  @ffi.free(address)
  @ffi.free(return_area)

  cleanup_list.each(@ffi.free)
  return ret
}

///|
/// Execute an arbitrary Redis command and receive the result.
pub fn Connection::execute(
  self : Connection,
  command : String,
  arguments : Array[RedisParameter],
) -> Result[Array[RedisResult], Error_] {
  let cleanup_list : Array[Int] = []

  let Connection(handle) = self

  let ptr = @ffi.str2ptr(command)

  let address = @ffi.malloc(arguments.length() * 16)
  for index = 0; index < arguments.length(); index = index + 1 {
    let iter_elem : RedisParameter = arguments[index]
    let iter_base = address + index * 16

    match iter_elem {
      Int64(payload) => {
        @ffi.store8(iter_base + 0, 0)
        @ffi.store64(iter_base + 8, payload)

        ()
      }
      Binary(payload0) => {
        @ffi.store8(iter_base + 0, 1)

        let ptr1 = @ffi.bytes2ptr(payload0)

        @ffi.store32(iter_base + 12, payload0.length())
        @ffi.store32(iter_base + 8, ptr1)
        cleanup_list.push(ptr1)

        ()
      }
    }
  }
  let return_area = @ffi.malloc(16)
  wasmImportMethodConnectionExecute(
    handle,
    ptr,
    command.length(),
    address,
    arguments.length(),
    return_area,
  )

  let lifted6 = match @ffi.load8_u(return_area + 0) {
    0 => {
      let array : Array[RedisResult] = []
      for index3 = 0; index3 < @ffi.load32(return_area + 8); index3 = index3 + 1 {
        let iter_base = @ffi.load32(return_area + 4) + index3 * 16

        let lifted = match @ffi.load8_u(iter_base + 0) {
          0 => RedisResult::Nil
          1 => {
            let result = @ffi.ptr2str(
              @ffi.load32(iter_base + 8),
              @ffi.load32(iter_base + 12),
            )

            RedisResult::Status(result)
          }
          2 => RedisResult::Int64(@ffi.load64(iter_base + 8))
          3 => {
            let result2 = @ffi.ptr2bytes(
              @ffi.load32(iter_base + 8),
              @ffi.load32(iter_base + 12),
            )

            RedisResult::Binary(result2)
          }
          _ => panic()
        }

        array.push(lifted)
      }
      @ffi.free(@ffi.load32(return_area + 4))

      Result::Ok(array)
    }
    1 => {
      let lifted5 = match @ffi.load8_u(return_area + 4) {
        0 => Error_::InvalidAddress
        1 => Error_::TooManyConnections
        2 => Error_::TypeError
        3 => {
          let result4 = @ffi.ptr2str(
            @ffi.load32(return_area + 8),
            @ffi.load32(return_area + 12),
          )

          Error_::Other(result4)
        }
        _ => panic()
      }

      Result::Err(lifted5)
    }
    _ => panic()
  }
  let ret = lifted6
  @ffi.free(ptr)
  @ffi.free(address)
  @ffi.free(return_area)

  cleanup_list.each(@ffi.free)
  return ret
}
